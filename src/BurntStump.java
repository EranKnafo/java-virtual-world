import processing.core.PImage;
import java.util.*;

public class BurntStump extends Entity {
    public BurntStump(String id, Point position, List<PImage> images) {
        super(id, position, images);
    }
}
