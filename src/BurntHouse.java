import processing.core.PImage;
import java.util.*;

public class BurntHouse extends Entity {
    
    public BurntHouse(String id, Point position, List<PImage> images) {
        super(id, position, images);
    }
}
