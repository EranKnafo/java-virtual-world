import processing.core.PImage;
import java.util.*;

public class BurntTree extends Entity {
    public BurntTree(String id, Point position, List<PImage> images) {
        super(id, position, images);
    }
}
